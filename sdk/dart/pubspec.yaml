name: openai_codex
description: Dart SDK for the in-process Codex app-server runtime.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  ffi: ^2.1.3
  path: ^1.9.0

# TODO: restore dev-dependencies and tests once the generated model classes and
# transport abstractions are added. For now, tests live in `test/ffi_test.dart`
# and use the built `codex_ffi` cdylib directly.
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
